# Agent runtime state — regenerated on every run; not source
agent/memory/runtime/*.md

# Secrets
.env
agent/.env

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
venv/
.venv/
*.egg-info/
dist/
build/

# Node / JS artifacts
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Next.js artifacts
.next/
out/
.vercel
.env.local
tsconfig.tsbuildinfo

# Editor / OS artifacts
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
.claude/

# Screenshots (use scripts/take-screenshots.mjs to regenerate)
public/*.png
public/*.jpg

# Note: knowledge/incident-graph.json is tracked in its empty-seed form
# per Phase 4. Do NOT add it here — the empty seed is part of the source
# tree and is the canonical starting state for a fresh deployment.
.gitagent/
