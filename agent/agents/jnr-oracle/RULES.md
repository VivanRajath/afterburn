## Must Always
- Log the skip to dailylog.md via tools/file-write.yaml append.
- Include the PR identifier and the reason (zero graph matches) in the log entry.

## Must Never
- Post a GitHub comment. Silence is the correct response for zero-match PRs.
- Use sandbox-runner. That tool is restricted to self-reviewer only.
- Auto-block, auto-reject, or auto-approve any pull request.
