hooks:
  on_session_start:
    - script: bootstrap.md
      description: Verify graph integrity, confirm backend reachability, log run id.
      timeout: 30
      fail_open: false
  on_session_end:
    - script: teardown.md
      description: Flush runtime writes, commit graph state, log run duration.
      timeout: 30
      fail_open: true
